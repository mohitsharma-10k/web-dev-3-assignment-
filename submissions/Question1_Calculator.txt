// =============================================================================
// Lab 1 - Question 1: CLI-Based Calculator using process.argv (1 mark)
// =============================================================================
// Requirements:
// - Create a calculator that accepts command-line arguments
// - Perform operations such as: Addition, Subtraction, Multiplication, Division
// - Example Command: node calculator.js add 10 5
// - Display output in terminal using console.log()
// - Handle invalid operations gracefully
// =============================================================================

// File: calculator.js
const args = process.argv.slice(2);
const operation = args[0];
const num1 = parseFloat(args[1]);
const num2 = parseFloat(args[2]);

// Input validation and error handling
if (!operation || isNaN(num1) || isNaN(num2)) {
    console.log("Usage: node calculator.js <operation> <num1> <num2>");
    console.log("Operations: add, subtract, multiply, divide");
    console.log("Example: node calculator.js add 10 5");
    process.exit(1);
}

// Perform calculations based on operation
switch (operation.toLowerCase()) {
    case 'add':
        console.log(`Result: ${num1 + num2}`);
        break;
    case 'subtract':
        console.log(`Result: ${num1 - num2}`);
        break;
    case 'multiply':
        console.log(`Result: ${num1 * num2}`);
        break;
    case 'divide':
        if (num2 === 0) {
            console.log("Error: Cannot divide by zero.");
        } else {
            console.log(`Result: ${num1 / num2}`);
        }
        break;
    default:
        console.log("Invalid operation. Please use add, subtract, multiply, or divide.");
}

/*
=============================================================================
SAMPLE TERMINAL COMMANDS & OUTPUT
=============================================================================
1) Addition:
   Command: node calculator.js add 10 5
   Output : Result: 15

2) Subtraction:
   Command: node calculator.js subtract 10 5
   Output : Result: 5

3) Multiplication:
   Command: node calculator.js multiply 10 5
   Output : Result: 50

4) Division:
   Command: node calculator.js divide 10 5
   Output : Result: 2

5) Division by Zero (Graceful Error Handling):
   Command: node calculator.js divide 10 0
   Output : Error: Cannot divide by zero.

6) Invalid Operation (Graceful Error Handling):
   Command: node calculator.js mod 10 5
   Output : Invalid operation. Please use add, subtract, multiply, or divide.

7) Missing Arguments:
   Command: node calculator.js
   Output : Usage: node calculator.js <operation> <num1> <num2>
            Operations: add, subtract, multiply, divide
            Example: node calculator.js add 10 5
=============================================================================
*/
