const crypto = require('crypto');

function rollDice() {
    const result = crypto.randomInt(1, 7); 
    console.log(`Dice Rolled: ${result}`);
}

console.log("Rolling dice 5 times using loops:");
for (let i = 1; i <= 5; i++) {
    rollDice();
}
